PORT=4174
# Интервал между проверками, мс (текущий режим — 1 минута).
CHECK_INTERVAL_MS=60000
# Случайная добавка к интервалу, мс (чтобы запросы не были ритмичными).
CHECK_JITTER_MS=15000
# Лимит проверок на задачу. 0 = без ограничения (мониторить до результата или дедлайна).
MAX_CHECKS_PER_TASK=0
# Минимальный интервал между уведомлениями об ошибках, мс.
TELEGRAM_MIN_INTERVAL_MS=60000
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Простой вход в панель (логин/пароль).
AUTH_LOGIN=Fedortsov
AUTH_PASSWORD=793356
